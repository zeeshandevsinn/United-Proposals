class SelectionModel {
  String? id;
  String? name;

  SelectionModel({
    this.id,
    this.name,
  });
}

class GenderModel {
  String? id;
  String? name;

  GenderModel({this.id, this.name});
}
